function [noisePowMat] = speechEStDD(noisy)
% Constants as per the reference paper
A=0;

epsilonMin = -25; % -25dB arbitrarily taken value as initial a priori SNR
epsilonEst = epsilonMin;
alpha=0.9




end
